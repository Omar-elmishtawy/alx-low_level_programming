#include "main.h"
#include <stdio.h>

/**
 * cap_string - test function
 * Description: test
 * @a: size
 * Return: jfds
 */

char *leet(char *)
{
	int i = 0;

	while (a[i] != '\0')
	{
		if (a[i] == 'a' || a[i] == 'A')
			a[i] = 4;
		if (a[i] == 'e' || a[i] == 'E')
			a[i] == 3;
		if (a[i] == 'o' || a[i] == 'O')
			a[i] == 0;
		if (a[i] == 't' || a[i] == 'T')
			a[i] = 7;
		if (a[i] == 'l' || a[i] == 'L')
			a[i] == 1;
		}
		i++;
	}

	return (a);
}
