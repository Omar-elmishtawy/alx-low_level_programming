#include "main.h"

/**
 * _atoi - this function to print _putchar
 * Description: we prin using _putchar function
 * @s: chracter to check in lower case or no
 * Return: 1 when it is lower case and 0 when otherwise
 */
int _atoi(char *s)
{
	return (0);
}
