#include "main.h"

/**
 * _islower - this function to print _putchar
 * Description: we prin using _putchar function
 */
int _islower(int c)
{
	if (c >= 97 && c<= 122)
	{
		return (1);
	}
	else
		return (0);
}
