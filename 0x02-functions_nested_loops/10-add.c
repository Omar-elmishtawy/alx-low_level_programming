#include "main.h"

/**
 * add - this function to print _putchar
 * Description: we prin using _putchar function
 * @x: chracter to check in lower case or no
 * @y: character two
 * Return: 1 when it is lower case and 0 when otherwise
 */
int add(int x, int y)
{
	return (x + y);
}
